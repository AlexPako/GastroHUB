Задание 6:
Использовал SELECT ... FOR UPDATE внутри транзакции.
Блокирует только строки кофеен с капучино, остальные заведения не трогает.
Другие транзакции не смогут менять эти строки пока наша не закончится.

Задание 7:
Использовал LOCK TABLE IN SHARE ROW EXCLUSIVE MODE.
Нашёл что этот режим запрещает INSERT/UPDATE/DELETE, но SELECT всё равно работает.
Это подходит под условие: "недоступна для изменений, но доступна для чтения".
